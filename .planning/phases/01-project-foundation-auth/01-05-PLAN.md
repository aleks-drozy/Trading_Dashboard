---
phase: 01-project-foundation-auth
plan: 05
type: execute
wave: 3
depends_on: ["01-01"]
files_modified:
  - components/landing/Navbar.tsx
  - components/landing/HeroSection.tsx
  - components/landing/FeaturesSection.tsx
  - app/page.tsx
autonomous: true
requirements:
  - UI-05

must_haves:
  truths:
    - "Landing page renders at / without authentication"
    - "Nav bar has logo left, Login + Get Started buttons right"
    - "Hero section has eyebrow, headline, subheading, and two CTAs"
    - "Features section has 3 feature cards in a grid"
    - "Get Started CTA links to /register"
    - "Login link navigates to /login"
  artifacts:
    - path: "components/landing/Navbar.tsx"
      provides: "Sticky nav bar with logo and auth links"
      contains: "TradeJournal"
    - path: "components/landing/HeroSection.tsx"
      provides: "Hero section with headline and CTAs"
      contains: "Track every trade"
    - path: "components/landing/FeaturesSection.tsx"
      provides: "3-column feature card grid"
      contains: "Log trades in seconds"
    - path: "app/page.tsx"
      provides: "Landing page composition"
      contains: "Navbar"
  key_links:
    - from: "components/landing/Navbar.tsx"
      to: "/register"
      via: "Get Started link"
      pattern: "/register"
    - from: "components/landing/Navbar.tsx"
      to: "/login"
      via: "Login link"
      pattern: "/login"
    - from: "components/landing/HeroSection.tsx"
      to: "/register"
      via: "Get Started CTA button"
      pattern: "/register"
---

<objective>
Build the polished landing page with Nav bar, Hero section, and Features section.

Purpose: First impression for new users. Per D-10, this is polished from day one, not a placeholder. The landing page is public (no auth required) and drives sign-ups.
Output: Complete landing page at / with sticky nav, hero with CTAs, and 3 feature cards.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-project-foundation-auth/01-CONTEXT.md
@.planning/phases/01-project-foundation-auth/01-UI-SPEC.md
@.planning/phases/01-project-foundation-auth/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Navbar, HeroSection, and FeaturesSection components</name>
  <files>
    components/landing/Navbar.tsx, components/landing/HeroSection.tsx,
    components/landing/FeaturesSection.tsx
  </files>
  <read_first>
    .planning/phases/01-project-foundation-auth/01-UI-SPEC.md (Landing Page section — Nav bar, Hero section, Features section, Feature cards — all specs)
    .planning/phases/01-project-foundation-auth/01-CONTEXT.md (D-09 through D-13: landing page decisions)
    app/globals.css (CSS variables for theme colors)
  </read_first>
  <action>
    1. Create `components/landing/Navbar.tsx`:
       - Sticky nav: `sticky top-0 z-50`, bg-[#1a1a1a], border-b border-[#2a2a2a], h-16 (64px)
       - Padding: px-8 (32px horizontal)
       - Layout: flex items-center justify-between
       - Logo (left): "Trade" in text-[#e5e7eb] + "Journal" in text-[#e5e7eb], with a "/" or "." character in text-[#00ff88] — e.g. "Trade/Journal" or "Trade.Journal" — text-2xl (24px) font-bold. Link to "/" via next/link
       - Nav right (per D-12): flex items-center gap-4
         - "Login" — next/link to /login, text-sm text-[#6b7280] hover:text-[#e5e7eb] transition-colors duration-150
         - "Get Started" — next/link to /register, bg-[#00ff88] text-[#0f0f0f] text-sm font-bold h-9 (36px) px-5 (20px) rounded-lg hover:bg-[#00e67a] transition-colors duration-150, inline-flex items-center

    2. Create `components/landing/HeroSection.tsx`:
       - Background: bg-[#0f0f0f]
       - Padding: py-24 (96px top/bottom) px-8 (32px horizontal)
       - Max content width: max-w-[720px] mx-auto, text-center
       - Eyebrow: "BUILT FOR ACTIVE TRADERS" — text-sm text-[#00ff88] uppercase tracking-widest (letter-spacing 0.08em) mb-4
       - Headline: "Track every trade. Improve every week." (per D-11) — text-5xl (48px) font-bold text-[#e5e7eb] leading-tight (line-height 1.1)
       - Subheading: "Log trades in under a minute. See your win rate, P&L, and R:R — instantly. No spreadsheets." — text-base text-[#6b7280] leading-relaxed (1.5) mt-6
       - CTA group: mt-10 flex flex-row items-center justify-center gap-4
         - Primary CTA: next/link to /register, styled as accent button — bg-[#00ff88] text-[#0f0f0f] text-base font-bold h-12 (48px) px-7 (28px) rounded-lg hover:bg-[#00e67a] transition-colors duration-150, inline-flex items-center. Text: "Get Started — it's free"
         - Secondary CTA: next/link to "#features" (anchor scroll to features section), no border, text-base text-[#6b7280] hover:underline h-12 px-4 inline-flex items-center. Text: "See how it works"

    3. Create `components/landing/FeaturesSection.tsx`:
       - `id="features"` (anchor target for "See how it works" link)
       - Background: bg-[#0f0f0f]
       - Padding: py-20 (80px) px-8 (32px)
       - Max content width: max-w-[1000px] mx-auto
       - Section label: "WHAT YOU GET" — text-sm text-[#00ff88] uppercase tracking-widest mb-4
       - Section heading: "Everything a serious trader needs" — text-2xl (24px) font-bold text-[#e5e7eb] mb-12 (48px)
       - Grid: grid grid-cols-1 md:grid-cols-3 gap-6 (24px)
       - Feature cards (3 total), each with:
         - Container: bg-[#1a1a1a] border border-[#2a2a2a] rounded-xl (12px) p-7 (28px) hover:border-[#3a3a3a] transition-colors duration-150
         - Icon: from lucide-react, 24px, text-[#00ff88], mb-4 (16px)
         - Title: text-2xl (24px) font-bold text-[#e5e7eb] mb-3 (12px)
         - Body: text-base text-[#6b7280] leading-relaxed (1.5)
       - Card 1: icon ClipboardList, title "Log trades in seconds", body "Capture every trade with symbol, entry, exit, P&L, notes, and chart screenshots — without breaking your focus."
       - Card 2: icon TrendingUp, title "Know your numbers", body "Win rate, profit factor, R:R, drawdown — calculated automatically from your closed trades."
       - Card 3: icon Monitor, title "A terminal built for traders", body "Dark interface optimized for long sessions. No distractions. No clutter. Just your trade data."
       - Import icons: `import { ClipboardList, TrendingUp, Monitor } from "lucide-react"`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - components/landing/Navbar.tsx contains "sticky top-0 z-50"
    - components/landing/Navbar.tsx contains "TradeJournal" or "Trade" (logo text)
    - components/landing/Navbar.tsx contains "#00ff88" (accent color for logo mark and Get Started button)
    - components/landing/Navbar.tsx contains "/login" (Login link)
    - components/landing/Navbar.tsx contains "/register" (Get Started link)
    - components/landing/HeroSection.tsx contains "BUILT FOR ACTIVE TRADERS" (eyebrow text, uppercase)
    - components/landing/HeroSection.tsx contains "Track every trade. Improve every week." (headline per D-11)
    - components/landing/HeroSection.tsx contains "Log trades in under a minute" (subheading)
    - components/landing/HeroSection.tsx contains "Get Started" (primary CTA)
    - components/landing/HeroSection.tsx contains "/register" (CTA destination)
    - components/landing/HeroSection.tsx contains "See how it works" (secondary CTA)
    - components/landing/FeaturesSection.tsx contains 'id="features"'
    - components/landing/FeaturesSection.tsx contains "ClipboardList" (lucide icon)
    - components/landing/FeaturesSection.tsx contains "TrendingUp" (lucide icon)
    - components/landing/FeaturesSection.tsx contains "Monitor" (lucide icon)
    - components/landing/FeaturesSection.tsx contains "Log trades in seconds" (card 1 title)
    - components/landing/FeaturesSection.tsx contains "Know your numbers" (card 2 title)
    - components/landing/FeaturesSection.tsx contains "A terminal built for traders" (card 3 title)
    - components/landing/FeaturesSection.tsx contains "grid-cols-1 md:grid-cols-3" (responsive grid)
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Three landing page components created with exact UI-SPEC styling: sticky Navbar with logo and auth links, HeroSection with eyebrow/headline/subheading/CTAs, FeaturesSection with 3-column card grid using lucide icons.</done>
</task>

<task type="auto">
  <name>Task 2: Compose landing page from components</name>
  <files>app/page.tsx</files>
  <read_first>
    components/landing/Navbar.tsx (import path)
    components/landing/HeroSection.tsx (import path)
    components/landing/FeaturesSection.tsx (import path)
    app/page.tsx (current placeholder content from Plan 01)
  </read_first>
  <action>
    Replace `app/page.tsx` with the composed landing page:

    ```typescript
    import { Navbar } from "@/components/landing/Navbar"
    import { HeroSection } from "@/components/landing/HeroSection"
    import { FeaturesSection } from "@/components/landing/FeaturesSection"

    export default function HomePage() {
      return (
        <main className="min-h-screen bg-[#0f0f0f]">
          <Navbar />
          <HeroSection />
          <FeaturesSection />
        </main>
      )
    }
    ```

    This is a server component (no "use client" needed) that composes the three landing page sections. The page is publicly accessible — proxy.ts does not block "/" since it only protects /dashboard and /api routes.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - app/page.tsx contains "Navbar" (import)
    - app/page.tsx contains "HeroSection" (import)
    - app/page.tsx contains "FeaturesSection" (import)
    - app/page.tsx does NOT contain '"use client"' (server component)
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Landing page at / composes Navbar + HeroSection + FeaturesSection. Page is publicly accessible, renders without auth, and builds successfully.</done>
</task>

</tasks>

<verification>
- `npm run build` exits with code 0
- app/page.tsx imports and renders all 3 landing components
- Landing page is publicly accessible (not blocked by proxy.ts)
</verification>

<success_criteria>
Polished landing page at / with sticky dark nav (logo + Login + Get Started), hero section with trader-focused copy per D-11, and 3-column feature card grid. All CTAs link to /register or /login. Page renders without authentication. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation-auth/01-05-SUMMARY.md`
</output>
