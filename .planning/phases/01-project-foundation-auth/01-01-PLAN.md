---
phase: 01-project-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - .env.example
  - .env.local
  - .gitignore
  - .husky/pre-commit
  - lint-staged.config.js
  - .prettierrc
  - .prettierignore
  - vitest.config.ts
  - public/favicon.ico
autonomous: true
requirements:
  - INFRA-04
  - INFRA-05

must_haves:
  truths:
    - "Project builds and starts with `npm run dev`"
    - "Pre-commit hook runs ESLint, Prettier, tsc, and Vitest on commit"
    - ".env.example documents all required environment variables"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "next"
    - path: ".husky/pre-commit"
      provides: "Pre-commit hook script"
      contains: "lint-staged"
    - path: "vitest.config.ts"
      provides: "Test runner configuration"
      contains: "vitest"
    - path: ".env.example"
      provides: "Environment variable documentation"
      contains: "MONGODB_URI"
    - path: "lint-staged.config.js"
      provides: "Lint-staged configuration"
      contains: "eslint"
  key_links:
    - from: ".husky/pre-commit"
      to: "lint-staged.config.js"
      via: "npx lint-staged"
      pattern: "lint-staged"
---

<objective>
Scaffold the Next.js 16 project from scratch and configure all dev tooling: TypeScript, Tailwind CSS, ESLint, Prettier, Husky pre-commit hooks, lint-staged, Vitest, and environment variable documentation.

Purpose: Establishes the project foundation that every subsequent plan builds on. Dev tooling ensures code quality from the first commit.
Output: A buildable Next.js 16 app with working pre-commit hooks and test runner.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation-auth/01-CONTEXT.md
@.planning/phases/01-project-foundation-auth/01-RESEARCH.md
@.planning/phases/01-project-foundation-auth/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 project and install all dependencies</name>
  <files>
    package.json, tsconfig.json, next.config.ts, tailwind.config.ts,
    app/layout.tsx, app/page.tsx, app/globals.css, .gitignore
  </files>
  <read_first>
    .planning/phases/01-project-foundation-auth/01-RESEARCH.md (standard stack versions, installation commands)
    .planning/phases/01-project-foundation-auth/01-UI-SPEC.md (font loading: Inter + JetBrains Mono, color tokens)
  </read_first>
  <action>
    1. Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --no-src-dir --import-alias="@/*"` in the project root (the project root already exists as a git repo with a docs/ folder). Accept defaults for remaining prompts. If create-next-app refuses to run in a non-empty directory, use `--yes` flag or create in a temp dir and move files.

    2. Install production dependencies:
       `npm install next-auth@beta mongoose bcryptjs resend zod`

    3. Install dev dependencies:
       `npm install -D @types/bcryptjs husky lint-staged prettier vitest lucide-react`

    4. Update `app/layout.tsx`:
       - Import Inter and JetBrains_Mono from `next/font/google`
       - Configure Inter: `const inter = Inter({ subsets: ['latin'], variable: '--font-inter' })`
       - Configure JetBrains Mono: `const jetbrainsMono = JetBrainsMono({ subsets: ['latin'], variable: '--font-mono' })`
       - Apply both font variables to the html element: `className={`${inter.variable} ${jetbrainsMono.variable} font-sans antialiased`}`
       - Set `<html lang="en" className="dark">`
       - Set metadata: `title: 'TradeJournal'`, `description: 'Track every trade. Improve every week.'`

    5. Update `app/globals.css` to include dark theme base styles:
       ```css
       @import "tailwindcss";

       :root {
         --background: #0f0f0f;
         --card: #1a1a1a;
         --accent: #00ff88;
         --destructive: #ef4444;
         --muted: #6b7280;
         --border: #2a2a2a;
         --text: #e5e7eb;
       }

       body {
         background-color: var(--background);
         color: var(--text);
         font-family: var(--font-inter), ui-sans-serif, system-ui, sans-serif;
       }
       ```

    6. Replace `app/page.tsx` with a minimal placeholder: `export default function Home() { return <main className="min-h-screen bg-[#0f0f0f]"><p className="text-[#e5e7eb] p-8">TradeJournal</p></main> }`

    7. Verify the project starts: `npm run dev` (confirm no errors in terminal output).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "next-auth" in dependencies
    - package.json contains "mongoose" in dependencies
    - package.json contains "bcryptjs" in dependencies
    - package.json contains "resend" in dependencies
    - package.json contains "zod" in dependencies
    - package.json contains "vitest" in devDependencies
    - package.json contains "husky" in devDependencies
    - package.json contains "lint-staged" in devDependencies
    - package.json contains "prettier" in devDependencies
    - package.json contains "lucide-react" in devDependencies
    - app/layout.tsx contains "Inter" and "JetBrains_Mono" font imports
    - app/globals.css contains "--background: #0f0f0f"
    - app/globals.css contains "--accent: #00ff88"
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Next.js 16 project builds successfully with all Phase 1 dependencies installed, dark theme CSS variables set, and Inter + JetBrains Mono fonts configured.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Husky, lint-staged, Prettier, Vitest, and .env.example</name>
  <files>
    .husky/pre-commit, lint-staged.config.js, .prettierrc, .prettierignore,
    vitest.config.ts, .env.example, .env.local
  </files>
  <read_first>
    package.json (verify dependencies installed from Task 1)
    .planning/phases/01-project-foundation-auth/01-RESEARCH.md (Pattern 7: Husky + lint-staged config, Pitfall 5: tsc outside lint-staged)
    .planning/phases/01-project-foundation-auth/01-CONTEXT.md (D-14, D-15, D-16 locked decisions)
  </read_first>
  <action>
    1. Initialize Husky: `npx husky init`

    2. Create `.husky/pre-commit` with exact content (per D-14, D-16 — tsc runs outside lint-staged):
       ```bash
       npx lint-staged
       npx tsc --noEmit
       npx vitest run --passWithNoTests
       ```

    3. Create `lint-staged.config.js` (per D-16 — targets `**/*.{ts,tsx}`):
       ```javascript
       module.exports = {
         "**/*.{ts,tsx}": [
           "eslint --fix",
           "prettier --write",
         ],
       }
       ```

    4. Create `.prettierrc` (per D-15 — auto-format, never fail):
       ```json
       {
         "semi": false,
         "singleQuote": false,
         "tabWidth": 2,
         "trailingComma": "es5",
         "printWidth": 100
       }
       ```

    5. Create `.prettierignore`:
       ```
       node_modules
       .next
       dist
       coverage
       ```

    6. Create `vitest.config.ts`:
       ```typescript
       import { defineConfig } from "vitest/config"
       import path from "path"

       export default defineConfig({
         test: {
           environment: "node",
           globals: true,
           include: ["__tests__/**/*.test.ts", "__tests__/**/*.test.tsx"],
         },
         resolve: {
           alias: {
             "@": path.resolve(__dirname, "."),
           },
         },
       })
       ```

    7. Create `.env.example` with all Phase 1 env vars (per INFRA-05):
       ```bash
       # MongoDB
       MONGODB_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/trade-journal

       # NextAuth
       NEXTAUTH_SECRET=generate-with-openssl-rand-base64-32
       NEXTAUTH_URL=http://localhost:3000
       AUTH_TRUST_HOST=true

       # Google OAuth (from Google Cloud Console)
       GOOGLE_CLIENT_ID=
       GOOGLE_CLIENT_SECRET=

       # Resend (from resend.com dashboard)
       RESEND_API_KEY=
       ```

    8. Create `.env.local` as a copy of `.env.example` with placeholder values (so the app can start without real credentials). Add `.env.local` to `.gitignore` if not already present.

    9. Add `"test"` script to package.json: `"test": "vitest run --passWithNoTests"`

    10. Run `npx vitest run --passWithNoTests` to confirm Vitest exits with code 0.
  </action>
  <verify>
    <automated>npx vitest run --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - .husky/pre-commit contains "npx lint-staged"
    - .husky/pre-commit contains "npx tsc --noEmit"
    - .husky/pre-commit contains "npx vitest run --passWithNoTests"
    - lint-staged.config.js contains "eslint --fix"
    - lint-staged.config.js contains "prettier --write"
    - lint-staged.config.js contains "**/*.{ts,tsx}"
    - vitest.config.ts contains "defineConfig"
    - vitest.config.ts contains "__tests__/**/*.test.ts"
    - .env.example contains "MONGODB_URI"
    - .env.example contains "NEXTAUTH_SECRET"
    - .env.example contains "NEXTAUTH_URL"
    - .env.example contains "GOOGLE_CLIENT_ID"
    - .env.example contains "GOOGLE_CLIENT_SECRET"
    - .env.example contains "RESEND_API_KEY"
    - .env.example contains "AUTH_TRUST_HOST"
    - .prettierrc exists
    - `npx vitest run --passWithNoTests` exits with code 0
  </acceptance_criteria>
  <done>Pre-commit hook runs lint-staged (ESLint + Prettier on staged .ts/.tsx files), tsc --noEmit globally, and Vitest. .env.example documents all 6 required env vars. Vitest config is ready for test files.</done>
</task>

</tasks>

<verification>
- `npm run build` exits with code 0
- `npx vitest run --passWithNoTests` exits with code 0
- `.husky/pre-commit` exists and contains all 3 commands
- `.env.example` contains all 6 env var keys
</verification>

<success_criteria>
Project scaffolded with Next.js 16, all dependencies installed, build passes, pre-commit hooks configured per D-14/D-15/D-16, Vitest runner configured, .env.example documents all required variables.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation-auth/01-01-SUMMARY.md`
</output>
