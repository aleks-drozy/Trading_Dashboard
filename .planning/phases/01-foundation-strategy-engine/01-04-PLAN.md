---
phase: 01-foundation-strategy-engine
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/reference/FYP_BOT_1_3.pine
  - tests/fixtures/spy_1min_tv_reference.csv
  - tests/fixtures/btcusdt_1min_tv_reference.csv
autonomous: false
requirements:
  - DATA-01
  - DATA-02

must_haves:
  truths:
    - "FYP_BOT_1_3.pine exists at docs/reference/FYP_BOT_1_3.pine and is committed to git"
    - "spy_1min_tv_reference.csv exists in tests/fixtures/ with at least 2 weeks of 1-minute bars including IFVG state, CISD state, and EMA value columns"
    - "btcusdt_1min_tv_reference.csv exists in tests/fixtures/ with the same columns and at least 2 weeks of 1-minute bars"
    - "Both CSV files have a minimum of 100 warmup bars before the comparison window (for EMA seeding)"
  artifacts:
    - path: "docs/reference/FYP_BOT_1_3.pine"
      provides: "PineScript source — strict strategy specification"
    - path: "tests/fixtures/spy_1min_tv_reference.csv"
      provides: "TradingView reference bars for SPY bar-by-bar validation"
    - path: "tests/fixtures/btcusdt_1min_tv_reference.csv"
      provides: "TradingView reference bars for BTCUSDT bar-by-bar validation"
  key_links:
    - from: "tests/test_strategy/"
      to: "tests/fixtures/spy_1min_tv_reference.csv"
      via: "pd.read_csv loaded in strategy test fixtures"
    - from: "backend/strategy/ifvg.py"
      to: "docs/reference/FYP_BOT_1_3.pine"
      via: "Logic extracted verbatim from Pine source"
---

<objective>
Commit the PineScript source file as the strategy specification, then export and commit TradingView reference CSV data for bar-by-bar validation. These two artifacts are hard blockers for Plan 05 — strategy engine tests cannot be written until both exist.

Purpose: The PineScript is the specification. The CSV fixtures are the test oracle. No strategy code is correct until it produces output matching these fixtures bar-by-bar.
Output: docs/reference/FYP_BOT_1_3.pine committed, two TradingView reference CSVs in tests/fixtures/.
</objective>

<execution_context>
@C:/Users/aleks/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/aleks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-strategy-engine/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit PineScript source to docs/reference/</name>
  <files>docs/reference/FYP_BOT_1_3.pine</files>
  <action>
The file `FYP_BOT_1_3.pine` already exists locally on the user's machine. The user specified its path in CONTEXT.md as the local file to commit.

1. Create the directory `docs/reference/` if it does not exist (it was created as an empty dir in Plan 01, but confirm).
2. Copy or move `FYP_BOT_1_3.pine` to `docs/reference/FYP_BOT_1_3.pine`. If the exact source path is unclear, prompt the user once for the current location of the file. Do NOT create a placeholder or stub — the real Pine source is required.
3. Verify the file is non-empty and has a `.pine` extension.
4. Stage and commit: `git add docs/reference/FYP_BOT_1_3.pine && git commit -m "docs(01): add PineScript strategy specification"`

The Pine source must NOT be modified or reformatted. It is a read-only specification.
  </action>
  <verify>
    <automated>cd C:/Users/aleks/projects/trading-dashboard && test -s docs/reference/FYP_BOT_1_3.pine && echo "Pine source exists and non-empty" || echo "MISSING"</automated>
  </verify>
  <done>docs/reference/FYP_BOT_1_3.pine exists, is non-empty, and is committed to git.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Export TradingView reference CSV fixtures</name>
  <files>
    tests/fixtures/spy_1min_tv_reference.csv,
    tests/fixtures/btcusdt_1min_tv_reference.csv
  </files>
  <action>Human must export TradingView reference CSVs — see what-to-do below for exact steps.</action>
  <what-to-do>
Export two reference CSV files from TradingView for bar-by-bar strategy validation. These are the test oracle for the strategy engine — accuracy of the engine is measured against these exact values.

**Step 1: Open TradingView and load the strategy**
- Open TradingView and load chart for **SPY** (1-minute timeframe)
- Load the `FYP_BOT_1_3` indicator/strategy from your Pine editor

**Step 2: Configure the export window**
- Set the chart date range to cover at least **2 full trading weeks** of 1-minute bars
- Include at least **100 warmup bars BEFORE** the start of your comparison window (needed for EMA seeding — see research Pitfall 2)
- Recommended: export 3 weeks total so there is buffer

**Step 3: Export the Data Window values**
From TradingView's Data Window (right panel), export the following columns per bar:
- `timestamp` (ISO 8601 or Unix ms)
- `open`, `high`, `low`, `close`, `volume`
- IFVG state column (Bullish / Bearish / None / Expired — use whatever the strategy outputs in the Data Window)
- CISD state column (Bullish / Bearish)
- EMA value (the 20-EMA numeric value)

**How to export:** In TradingView, right-click the chart -> "Download chart data" or use the Pine script's `table.cell()` Data Window export. If the strategy outputs a Status Table visible in the Data Window, capture those values. If direct CSV export is not available, use TradingView's "Export chart data" button (bottom of chart area -> "Export data to CSV").

**Step 4: Repeat for BTCUSDT**
- Switch chart to **BTCUSDT** (1-minute)
- Same indicator, same date range, same columns
- Export second CSV

**Step 5: Save files**
Save both files to:
- `tests/fixtures/spy_1min_tv_reference.csv`
- `tests/fixtures/btcusdt_1min_tv_reference.csv`

**Required CSV format (header row):**
```
timestamp,open,high,low,close,volume,ifvg_state,cisd_state,ema_20
```

**Validation checks before resuming:**
- Both files exist and are non-empty
- Each file has > 500 rows (2+ weeks of 1-min bars)
- The `ifvg_state` column contains values like: Bullish, Bearish, None, or Expired
- The `cisd_state` column contains: Bullish or Bearish
- The `ema_20` column contains numeric values (not empty)
  </what-to-do>
  <verify>
    <automated>cd C:/Users/aleks/projects/trading-dashboard && python -c "import pandas as pd; spy=pd.read_csv('tests/fixtures/spy_1min_tv_reference.csv'); btc=pd.read_csv('tests/fixtures/btcusdt_1min_tv_reference.csv'); print('SPY rows:', len(spy), '| BTCUSDT rows:', len(btc)); assert len(spy) > 500 and len(btc) > 500, 'Need > 500 rows each'"</automated>
  </verify>
  <done>Both fixture CSVs exist in tests/fixtures/ with 500+ rows and all required columns (timestamp, OHLCV, ifvg_state, cisd_state, ema_20).</done>
  <resume-signal>
Type "fixtures ready" once both CSV files are saved to tests/fixtures/. If you encountered issues with the export format, describe what columns you were able to export and Claude will adapt the fixture loader accordingly.
  </resume-signal>
</task>

</tasks>

<verification>
After checkpoint resumes:
- `test -s tests/fixtures/spy_1min_tv_reference.csv` — non-empty
- `test -s tests/fixtures/btcusdt_1min_tv_reference.csv` — non-empty
- `python -c "import pandas as pd; df = pd.read_csv('tests/fixtures/spy_1min_tv_reference.csv'); print(len(df), 'rows', list(df.columns))"`
- Confirm columns include timestamp, OHLCV, ifvg_state, cisd_state, ema_20 (or equivalent names)
- Confirm row count > 500
</verification>

<success_criteria>
- docs/reference/FYP_BOT_1_3.pine committed to git (non-empty)
- tests/fixtures/spy_1min_tv_reference.csv with 500+ rows, all required columns
- tests/fixtures/btcusdt_1min_tv_reference.csv with 500+ rows, all required columns
- Both CSVs loadable with pd.read_csv without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-strategy-engine/01-04-SUMMARY.md`
</output>
